# OpenGuard Configuration
# =======================
# This file defines the security guards applied to LLM traffic.
# The default configuration below is empty, allowing all traffic to pass through.
#
# Copy this file to guards.yaml and uncomment the example below to get started.
# See `presets/full.yaml` for the configuration used in integration tests.

guards: []

# Example configuration logic you can uncomment:
# guards:
#   - match:
#       model:
#         _ilike: "%gpt-4%"
#     apply:
#       - type: pii_filter
#         config:
#           enabled: true
