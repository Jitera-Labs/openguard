# @import ../variables.http

### Health Check
GET {{host}}/health
?? status == 200
?? js response.parsedBody.status == ok

### Root Info
GET {{host}}/

?? status == 200
?? js response.parsedBody.name == OpenGuard
?? js response.parsedBody.endpoints.health == /health
?? js response.parsedBody.endpoints.models == /v1/models
?? js response.parsedBody.endpoints.chat == /v1/chat/completions
